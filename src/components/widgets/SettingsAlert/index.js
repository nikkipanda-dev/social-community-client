import { styled } from "../../../stitches.config";

import Text from "../../core/Text";

const SettingsAlertWrapper = styled('div', {});

export const SettingsAlert = ({ isAuth, }) => {
    return (
        <SettingsAlertWrapper>
            Soon
        </SettingsAlertWrapper>
    )
}

export default SettingsAlert;