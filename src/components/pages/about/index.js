import { styled } from "../../../stitches.config";

const AboutWrapper = styled('div', {});

export const About = () => {
    return (
        <AboutWrapper>
            About
        </AboutWrapper>
    )
}

export default About;